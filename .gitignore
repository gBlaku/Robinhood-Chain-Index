# working directory: scan database, logs, checkpoints
out/

# python
.venv/
__pycache__/
*.pyc
BRIEF.md
# (BRIEF.md is your private working brief - excluded from the repo)

# full table ships gzipped; the raw csv is 55MB and GitHub cannot preview it
data/all_tokens.csv
# full-chain launcher table ships gzipped; raw is ~86MB
data/launchers_full.csv
